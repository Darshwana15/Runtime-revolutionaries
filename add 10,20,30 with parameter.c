#include <stdio.h>
void add(int x, int y, int z) 
{
    int sum = x + y + z;
    printf("Sum of %d, %d, and %d is: %d\n", a, b, c, sum);
}

int main() 
{
    add(10, 20, 30); 
    return 0;
}
